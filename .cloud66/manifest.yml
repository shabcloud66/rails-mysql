development:
    rails: 
        servers: 
            server:
                unique_name: meeh
                extra_packages: 
                    - atop
                    - chrony
        configuration:
            do_initial_db_schema_load: true
staging:
    rails: 
        servers: 
            server:
                unique_name: meeh
                extra_packages: 
                    - atop
                    - chrony
        configuration:
            do_initial_db_schema_load: true           
production:
    rails: 
        servers: 
            server:
                unique_name: meeh
                extra_packages: 
                    - atop
                    - chrony
        configuration:
            do_initial_db_schema_load: true
